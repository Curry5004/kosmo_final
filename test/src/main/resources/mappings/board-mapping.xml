<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BoardDAO">


<select id ="getBoardList" resultType="board">
	SELECT * FROM BOARD 
	WHERE ART_AVAILABLE = TRUE AND PARTY_ID = #{party_id}
	ORDER BY ART_ID DESC 
	LIMIT #{listSize} OFFSET #{startRow}
</select>
<select id="getBoardCnt" resultType="int">
	SELECT COUNT(*) FROM BOARD 
	WHERE ART_AVAILABLE = TRUE AND PARTY_ID = #{party_id}
</select>
<insert id="insertBoard" parameterType="board">
	insert into board(party_id, art_title,art_writer,art_content, art_img_path)
	values('1',#{art_title},'kms',#{art_content}, #{art_img_path})
</insert>

<update id="update Board" parameterType="board">
	update board set art_title=#{art_title}, art_content=#{art_content} where party_id='1'
</update>

<delete id="delete Board" parameterType="board">
	delete board where party_id='1'
</delete>


</mapper>